import { Link } from "react-router-dom";

const HeaderBtn = () => {
  return (
    <Link to="/features">
      <button id="primary">Try For Free!</button>
    </Link>
  );
};

export default HeaderBtn;
